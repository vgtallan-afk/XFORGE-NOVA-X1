# HTML Editor

Responsive carousel update:
- Removed tap/click-to-enlarge lightbox behavior.
- Carousel can be resized in the sidebar.
- Mobile: one screenshot at a time, swipe left/right.
- Desktop: multiple screenshots side by side when space allows, remaining slides continue horizontally.
- Desktop can be navigated with mouse drag, arrows, and dots.
